### Test Host Statistics API

# Lấy thống kê cho host hiện tại (tháng này)
GET http://localhost:8080/api/hosts/my-statistics?period=current_month
Authorization: Bearer YOUR_JWT_TOKEN_HERE

###

# Lấy thống kê cho host hiện tại (tháng trước)
GET http://localhost:8080/api/hosts/my-statistics?period=last_month
Authorization: Bearer YOUR_JWT_TOKEN_HERE

###

# Lấy thống kê cho host hiện tại (3 tháng gần đây)
GET http://localhost:8080/api/hosts/my-statistics?period=last_3_months
Authorization: Bearer YOUR_JWT_TOKEN_HERE

###

# Lấy thống kê cho host hiện tại (6 tháng gần đây)
GET http://localhost:8080/api/hosts/my-statistics?period=last_6_months
Authorization: Bearer YOUR_JWT_PORT_HERE

###

# Lấy thống kê cho host hiện tại (năm nay)
GET http://localhost:8080/api/hosts/my-statistics?period=current_year
Authorization: Bearer YOUR_JWT_TOKEN_HERE

###

# Lấy thống kê cho host cụ thể (cần quyền admin)
GET http://localhost:8080/api/hosts/1/statistics?period=current_month
Authorization: Bearer YOUR_JWT_TOKEN_HERE
